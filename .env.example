# Opt in to embedding resume PDF text in public/data/profile-context.json at prebuild time.
# Requires Resume_Aug2026.2.pdf in the repo root (gitignored). Leave unset for public builds.
# INCLUDE_RESUME_IN_PROFILE_CONTEXT=1
